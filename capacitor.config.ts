import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'enclosure.calculator',
  appName: 'enclosure-calculator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
